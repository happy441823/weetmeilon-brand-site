# 内容发布流程

统一流程：

```text
草稿 -> 待审核 -> 立即发布或定时发布 -> 更新 -> 下线 -> 归档
```

权限：

- editor：编辑并提交审核。
- reviewer：审核、发布、下线。
- super_admin：全部操作。
- viewer：只读查看。

定时发布有两层保障：后台任务负责状态刷新，公开查询始终检查发布时间，任务延迟也不会提前公开内容。

